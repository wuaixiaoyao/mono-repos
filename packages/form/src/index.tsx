import React, { PureComponent } from 'react'
import Button from "@wuaixiaoyao-lib/button";

const Form = (props) => {
    return <div>
        form
        <Button>save</Button>
    </div>
}

export default Form