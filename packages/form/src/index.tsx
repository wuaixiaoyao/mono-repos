import React, { PureComponent } from 'react'
import Button from "@wuaixiaoyao-lib/button";

const Form = (props) => {
    return <div>
        form
        <Button>点击保存</Button>
    </div>
}

export default Form